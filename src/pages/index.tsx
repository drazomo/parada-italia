import { openSans } from '@/styles/constants';

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col">
      /* HEADER */
      <main className={`${openSans.variable} font-body flex-grow`}>
        helo world
      </main>
      /* Footer */
    </div>
  );
}
